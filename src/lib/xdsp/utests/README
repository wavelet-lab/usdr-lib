==XDSP conversion functions unit tests suite.==

You can restrict the test subset specifying special tags.
Regression tests are tagged as REGRESS.
Performance tests are tagged as PERFORMANCE.
You can select tags by setting environment variables CK_INCLUDE_TAGS and CK_EXCLUDE_TAGS.

For example:

Run all tests:
./xdsp_utest_suite
or
CK_INCLUDE_TAGS="REGRESS PERFORMANCE" ./xdsp_utest_suite

Run only regression tests:
CK_INCLUDE_TAGS=REGRESS ./xdsp_utest_suite
or
CK_EXCLUDE_TAGS=PERFORMANCE ./xdsp_utest_suite

Run only performance tests:
CK_INCLUDE_TAGS=PERFORMANCE ./xdsp_utest_suite
or
CK_EXCLUDE_TAGS=REGRESS ./xdsp_utest_suite
